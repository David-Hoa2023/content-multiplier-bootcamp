[variables]
NPM_CONFIG_PRODUCTION = 'false'

[phases.setup]
nixPkgs = ['nodejs_18', 'npm']

[phases.install]
cmds = [
  'cd backend && npm ci',
  'cd frontend && npm ci'
]

[phases.build] 
cmds = ['cd frontend && npm run build']

[start]
cmd = 'bash start.sh'